server:
  port: 9000

zuul:
  ignoredServices: '*'
  routes:
    composite:
      path: /composite/**
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000
